name: Deploy Application

on:
  push:
              echo "needs_setup=false" >> $GITHUB_OUTPUT
            echo "✅ Setup complete: ECR repository exists in AWS"
            
            # Verificar se variáveis necessárias estão definidas
            if [ -z "${{ vars.APP_NAME }}" ]; then
              echo "🔧 Setting APP_NAME variable..."
              gh variable set APP_NAME --body "${{ vars.ECR_REPOSITORY }}"
            fi
            
            if [ -z "${{ vars.ECR_REGISTRY }}" ]; then
              echo "🔧 Setting ECR_REGISTRY variable..."
              gh variable set ECR_REGISTRY --body "124213954422.dkr.ecr.us-east-1.amazonaws.com"
            fibranches:
      - main
      - development
      
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  id-token: write

jobs:
  setup-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    outputs:
      needs_setup: ${{ steps.check.outputs.needs_setup }}
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Check if setup is needed
        id: check
        env:
          GH_TOKEN: ${{ secrets.GH_ADMIN_TOKEN }}
        run: |
          if [ -z "${{ vars.ECR_REPOSITORY }}" ]; then
            echo "needs_setup=true" >> $GITHUB_OUTPUT
            echo "🔧 Setup needed: ECR_REPOSITORY variable not found"
          else
            echo "🔍 ECR_REPOSITORY variable exists: ${{ vars.ECR_REPOSITORY }}"
            echo "🔍 Checking if ECR repository exists in AWS..."
            
            # Check if ECR repository actually exists
            if aws ecr describe-repositories --repository-names "${{ vars.ECR_REPOSITORY }}" --region us-east-1 >/dev/null 2>&1; then
              echo "needs_setup=false" >> $GITHUB_OUTPUT
              echo "✅ Setup complete: ECR repository exists in AWS"
            else
              echo "needs_setup=true" >> $GITHUB_OUTPUT
              echo "❌ Setup needed: ECR repository '${{ vars.ECR_REPOSITORY }}' does not exist in AWS"
              echo "🛠️  Please create the ECR repository manually: aws ecr create-repository --repository-name ${{ vars.ECR_REPOSITORY }} --region us-east-1"
              exit 1
            fi
          fi

  deploy:
    needs: [setup-check]
    if: needs.setup-check.outputs.needs_setup == 'false'
    uses: LinkedFarma/workflows/.github/workflows/argo-cd.yml@master
    with:
      environment: dev
    secrets: inherit
